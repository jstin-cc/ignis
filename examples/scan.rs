//! examples/scan.rs — CLI entry for iterating the JSONL scanner during
//! development. Fleshed out in Phase 1 once the scanner lands.

fn main() {
    eprintln!("winusage-core scan example — not yet implemented (Phase 1).");
}
